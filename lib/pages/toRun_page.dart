import 'package:flutter/cupertino.dart';

class ToRunPage extends StatelessWidget {
  const ToRunPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Correr'),
    );
  }
}
